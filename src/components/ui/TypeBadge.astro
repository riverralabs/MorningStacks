---
import { typeLabel, type ArticleType } from '~/lib/content-model';

type Props = {
  type: ArticleType;
};

const { type } = Astro.props;
---

<span class="font-sans text-[11px] font-bold uppercase tracking-[0.14em] text-[var(--color-ink)]">
  {typeLabel(type)}
</span>
